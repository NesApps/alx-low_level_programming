#include <stdio.h>
/**
 * main - A program the prints a line using the print function
 * Return: Always 0 (success)
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
